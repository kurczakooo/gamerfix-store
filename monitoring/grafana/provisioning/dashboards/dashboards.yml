apiVersion: 1

providers:
  - name: GamerFix
    orgId: 1
    folder: GamerFix
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true

    options:
      path: /var/lib/grafana/dashboards